{
  "enabled": true,
  "name": "Shell 指令安全檢查器",
  "description": "攔截 shell 工具呼叫，檢查是否包含危險指令組合（如 rm 搭配 / 或 ..、遞迴刪除、格式化指令等），若偵測到則暫停並詢問使用者確認後才執行。",
  "version": "1",
  "when": {
    "type": "preToolUse",
    "toolTypes": [
      "shell"
    ]
  },
  "then": {
    "type": "askAgent",
    "prompt": "你是一個指令安全檢查器。請檢查即將執行的 shell 指令，判斷是否包含以下潛在危險模式：\n\n## 需要提醒使用者確認的模式：\n1. `rm` 指令搭配路徑包含 `/` 或 `..`（例如 `rm -rf /`、`rm ../file`）\n2. `rm -rf` 或 `rm -r` 搭配根目錄或上層目錄\n3. 使用 `&&`、`||`、`;`、`|` 等串接運算子將 `cd` 與 `rm` 組合（例如 `cd folder1 && rm file1.txt`）\n4. `mkfs`、`dd if=` 等格式化/覆寫磁碟指令\n5. `chmod 777` 搭配遞迴 `-R` 應用於系統目錄\n6. `> /dev/sda` 或類似的裝置覆寫\n7. `:(){:|:&};:` fork bomb\n8. `curl | sh` 或 `wget | bash` 等未經驗證的遠端腳本執行\n\n## 判斷規則：\n- 如果指令包含上述任何危險模式，回覆：「⚠️ 偵測到潛在危險指令：[具體說明哪個模式]。請向使用者確認是否要執行此指令，等待使用者明確同意後才可繼續。」\n- 如果指令安全（例如單純的 `ls`、`cat`、`mkdir`、`npm run`、`dotnet build` 等），回覆：「✅ 指令安全，允許執行。」\n\n重要：不要使用「拒絕」、「禁止」、「denied」等字眼。危險指令應該提醒確認，而非直接阻擋。"
  }
}
